import { BookmarkStyleConfig } from '../../types/renderer';

export const vellumBookmarkStyleConfig: BookmarkStyleConfig = {
  isDarkTheme: false,
  backgroundColor: '#ffffff',
  strokeColor: '#000000',
  mutedColor: '#777777',
};

export const spaceBookmarkStyleConfig: BookmarkStyleConfig = {
  isDarkTheme: true,
  backgroundColor: '#000000',
  strokeColor: '#ffffff',
  mutedColor: '#888888',
};
